<%# @userがnilでない場合（プロフィール画面の場合）は何もせず、nilの場合（Homeページの場合）はcurrent_userを代入する %>
<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">
      <%# 下記のコードはDBにSQLを発行して計算させている（Rubyに計算させるよりDBにさせた方が高速化するため） %>
      <%= @user.following.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stats">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
</div>
