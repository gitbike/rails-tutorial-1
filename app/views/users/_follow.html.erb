<%# form_for()メソッドはRerationshipモデルオブジェクトを操作するメソッド。POSTリクエストをRelationshipsコントローラに送信してRerationshipをcreateしている %>
<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <%# ブラウザに表示させたくがないが、relationships表の外部キーfollowed_idをRelationshipsコントローラのdestoroyアクションに送信するためのメソッド %>
  <div><%= hidden_field_tag(:followed_id, @user.id) %></div>
  <%= f.submit 'Follow', class: 'btn btn-primary' %>
<% end %>